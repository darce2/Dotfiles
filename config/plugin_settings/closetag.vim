let g:closetag_filenames = '*.html,*.xhtml,*.phtml,*js,*jsx,*tsx'          "files to run close tag on

" Add > at current position without closing the current tag
let g:closetag_close_shortcut = '<leader>>'

